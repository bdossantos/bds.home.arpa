---

morning:
  alias: 'Morning routine'
  sequence:
    - service: light.turn_on
      entity_id: light.bedroom_lights
      data:
        transition: 1200
        brightness_pct: 30
        xy_color: [0.561, 0.404]
    - delay: "{{ delay or '00:00:30' }}"
